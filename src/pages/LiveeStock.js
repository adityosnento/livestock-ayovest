import React, { Component } from "react";
import LiveStock from "../components/liveStock/LiveStock";

export default class LiveeStock extends Component {
  render() {
    return (
      <div>
        <LiveStock />
      </div>
    );
  }
}
